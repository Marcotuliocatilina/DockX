<script>
// Alias to the new MainPage component — keeps the old filename working while the project
// now uses `MainPage.vue`. This helps during the rename transition.
import MainPage from './MainPage.vue'
export default MainPage
</script>

